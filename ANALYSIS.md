# 분석 노트

받은 피드백 3건에 대한 답입니다. 기능 설명은 [OVERVIEW.md](OVERVIEW.md)를 보세요.

측정 재현: `npm run analyze -- <layer> <head>`

---

## 1. 어떤 모델의, 어떤 층의, 어떤 헤드인가

`google/bert_uncased_L-2_H-128_A-2` (통칭 **bert-tiny**)

| | |
|---|---|
| hidden | 128 |
| 층 | 2 (layer 0, 1) |
| 헤드 | 2 (head 0, 1) |
| head_dim | 64 |
| 어휘 | 30,522 (WordPiece) |

화면 상단에 모델 ID가 표시되고, **Layer / Head 드롭다운**으로 선택합니다. Q·K·V 단계 제목에도 `layer 0, head 0`이 함께 나옵니다.

가중치는 HuggingFace 원본 체크포인트에서 직접 추출했습니다. 추론 라이브러리를 거치지 않으므로 화면의 모든 숫자가 체크포인트 값에서 직접 계산됩니다.

**해당 텐서:**

```
bert.encoder.layer.{L}.attention.self.query.weight   [128, 128]
bert.encoder.layer.{L}.attention.self.key.weight     [128, 128]
bert.encoder.layer.{L}.attention.self.value.weight   [128, 128]
bert.encoder.layer.{L}.attention.output.dense.weight [128, 128]   ← W_O
```

헤드 $h$는 이 행렬의 열 $[64h,\ 64h+64)$ 구간입니다. 즉 128차원 투영 하나를 2개 헤드가 64차원씩 나눠 씁니다.

---

## 2. 계산 규약: $XW_Q$ 인가 $W_QX^T$ 인가

**$XW_Q$ 로 통일했습니다.**

$$X \in \mathbb{R}^{n \times 128}, \quad W_Q \in \mathbb{R}^{128 \times 64}, \quad Q = XW_Q + b_Q \in \mathbb{R}^{n \times 64}$$

$n$ = 토큰 수. 행이 토큰, 열이 차원입니다. 화면의 모든 표가 이 방향(행=토큰)으로 그려집니다.

### 주의할 점 — PyTorch는 반대로 저장합니다

PyTorch `nn.Linear`는 가중치를 **`[out_features, in_features]`** 로 저장하고 내부적으로 $xW^T$ 를 계산합니다. 즉 체크포인트의 `query.weight`는 우리 규약 기준으로 **이미 전치된 상태**입니다.

그래서 **추출 시점에 한 번 전치**해서 `[in, out]`으로 저장했습니다. 덕분에 앱 코드는 전치 없이 그대로 곱하면 됩니다.

```
체크포인트  [out, in]  ──전치──▶  에셋  [in, out]  ──▶  X · W_Q
```

이 전치가 맞는지는 `npm run model:verify`가 원본과 원소 단위로 대조합니다 (오차 0).

> 이 부분을 틀리면 에러 없이 그럴듯한 숫자가 계속 나옵니다. 그래서 빈칸 맞히기(MLM) 결과를 최종 검증으로 씁니다 — 전치가 틀렸다면 예측이 무작위 단어로 무너집니다.

---

## 3. Q, K, V는 정말 다르게 매핑되는가

**측정 방법:** 흔한 영단어 3,000개를 각각 $W_Q, W_K, W_V$ 로 투영한 뒤 두 가지를 쟀습니다.

- **이웃 겹침** — 단어별 top-10 최근접 목록이 두 공간 사이에서 얼마나 겹치는가 (Jaccard)
- **유사도 구조 상관** — 무작위 단어쌍 4,000개의 코사인 유사도가 두 공간에서 얼마나 같이 움직이는가 (Pearson $r$)

**기준선:** 서로 무관한 무작위 투영 2개를 같은 방법으로 측정했습니다. 이 값보다 높아야 "관련 있다"고 말할 수 있습니다.

### 결과

| 비교 | 이웃 겹침 | 유사도 상관 |
|---|---|---|
| **Q vs K** | **0.520** | **r = 0.838** |
| Q vs V | 0.169 | r = 0.481 |
| K vs V | 0.196 | r = 0.457 |
| *무작위 기준선* | *0.184* | *r = 0.414* |

<sub>layer 0, head 0. 나머지 3개 (층, 헤드) 조합도 같은 패턴 — Q–K가 항상 가장 높고 기준선 위, Q–V와 K–V는 기준선 근처.</sub>

### 해석

**Q와 K는 비슷하게, V만 다르게 매핑됩니다.**

- Q–K는 기준선(0.184)의 약 3배로 겹칩니다. 두 투영이 사실상 비슷한 방향으로 단어를 늘어놓습니다.
- Q–V, K–V는 기준선과 거의 구별되지 않습니다.

말이 되는 결과입니다. Q와 K는 **같은 계산**($q\cdot k$, 어디를 볼지)에 함께 들어가고, V는 **다른 계산**(무엇을 전달할지)에 들어가기 때문입니다.

---

## 3-1. 그런데 개별 $W_Q$는 애초에 결정되지 않습니다

위 결과보다 중요한 사실입니다.

Attention 점수는 항상 이 형태로만 등장합니다.

$$\text{score}_{ij} = x_i W_Q W_K^T x_j^T$$

$W_Q$ 와 $W_K$ 가 **곱으로만** 나타납니다. 그러면 아무 가역행렬 $R$ 에 대해

$$W_Q \rightarrow W_Q R, \qquad W_K \rightarrow W_K R^{-T}$$

로 바꿔도 $W_Q R R^{-1} W_K^T = W_Q W_K^T$ 이므로 **모델의 동작이 완전히 동일합니다.**

### 수치로 확인

무작위 가역행렬 $R$ 로 위 치환을 적용한 결과:

```
attention 점수의 최대 변화     1.5e-5      ← 부동소수점 오차
Q 공간의 이웃 목록 겹침         0.487       ← 절반이 바뀜
```

**모델은 그대로인데 "Q의 이웃"은 마음대로 달라집니다.**

즉 결정되는 것은 곱 $W_Q W_K^T$ 뿐이고, $W_Q$ 단독은 결정되지 않습니다. $W_V$ 와 $W_O$ 도 같은 이유로 곱 $W_V W_O$ 만 결정됩니다.

> 이 관점을 QK circuit / OV circuit이라 부릅니다.
> Elhage et al., *A Mathematical Framework for Transformer Circuits* (2021)
>
> - $W_Q W_K^T$ = **QK circuit** — 어디에 주목할지 결정
> - $W_V W_O$ = **OV circuit** — 주목한 곳에서 무엇을 가져올지 결정

### 이 결론이 앱의 설계를 결정했습니다

처음에는 "Q 이웃 / K 이웃 / V 이웃" — 각 공간에서 코사인 유사도로 최근접 단어를 뽑는 기능이었습니다. 그런데 그 목록은 개별 $W_Q$에 의존하므로 **위 자유도의 영향을 받습니다.** 같은 모델을 다르게 매개변수화하면 결과가 달라집니다.

그래서 전부 **곱을 통해 읽는 방식**으로 바꿨습니다.

| 판독 | 방법 | 쓰는 곱 |
|---|---|---|
| **Q** — 내가 찾는 것 | 내 Query에 가장 잘 걸리는 Key를 가진 단어 | $W_QW_K^T$ |
| **K** — 나를 찾는 것 | 내 Key를 가장 잘 집어내는 Query를 가진 단어 | $W_QW_K^T$ |
| **V** — 내가 전하는 것 | $v \cdot W_O$ 를 MLM 헤드로 디코딩 | $W_VW_O$ |

셋 다 결정되는 양만 사용하므로 매개변수화와 무관합니다. 게다가 결과가 훨씬 읽힙니다 — `visual`의 Q는 `visualization, visualised, visualizes`를, V는 `visual, vision, visually`를 가리킵니다.

---

## 정리

| 피드백 | 답 |
|---|---|
| **어떤 모델/층/헤드?** | bert-tiny, 화면에서 Layer·Head 선택. 원본 체크포인트에서 직접 추출 |
| **$XW_Q$ vs $W_QX^T$?** | $XW_Q$ (X는 토큰수×128). PyTorch가 `[out, in]`으로 저장하므로 추출 시 전치 |
| **Q/K/V가 정말 다른가?** | **Q≈K, V만 다름.** 단, 개별 $W_Q$는 결정되지 않으며 곱 $W_QW_K^T$ 만 의미를 가짐 |
